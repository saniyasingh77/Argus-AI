# Deployable web-tier dependencies (Vercel / Render / Docker / CI).
# Deliberately tiny: the UI, auth, SQLite reports and alerts dashboard need
# only these. The camera / video CV features (OpenCV, MediaPipe, TensorFlow)
# are local-hardware only and live in requirements-ml.txt.
Flask==3.0.3
python-dotenv==1.0.1
gunicorn==22.0.0
